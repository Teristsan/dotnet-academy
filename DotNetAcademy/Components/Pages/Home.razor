@page "/"
@using DotNetAcademy.Components.ItemComponents
@using DotNetAcademy.Enums
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize]
@rendermode InteractiveServer

<div class="d-flex flex-column justify-content-center align-items-center">
    <PageTitle>Home</PageTitle>

    <ItemCompList MediaType="@mediaType" />

    
</div>

@code {
    // Default media type passed to the child component.
    // Change the type/value if your ItemCompList expects an enum or another type.
    private MediaTypeEnum mediaType = MediaTypeEnum.Movie;
}